import { StyleSheet } from 'react-native';

export default styles = StyleSheet.create({
  flex: {},
  activityIndicator: {
    flex: 1,
    alignItems: 'center',
    justifyContent: 'center',
  },
});